export type category = {
	id: number
	title: string
	image: string
	tag: string
}

export type product = {
	id: number
	title: string
	price: number
	image: string
}

export type gameMode = {
	id: number
	title: string
	numPlayers: number
}
